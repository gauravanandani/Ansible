---
 - hosts: localhost
   tasks:
   - name: remove anonymos user  
     mysql_user:
        name: ''
        host_all: yes
        state: absent 
        login_user: root
        login_password: redhat
